namespace Scripts
{
    /// <summary>
    /// Enum for checking if an Agent is Hungry, Tired or neither (Normal).
    /// </summary>
    public enum AgentStat
    {
        Normal,
        Hungry,
        Tired
    }
}